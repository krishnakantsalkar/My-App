import { Route } from '@angular/router';
import { SearchByTagComponent } from './search-by-tag/search-by-tag.component';

export const searchByTagRoutes: Route[] = [
  {
    path: '',
    component: SearchByTagComponent,
  },
];
